<!-- File: SRS - Phần 3 -->

# CHƯƠNG 4. MÔ HÌNH ĐỘNG CỦA HỆ THỐNG

## 4.1. Mục tiêu

Chương này trình bày các mô hình động (Dynamic Models) mô tả cách các đối tượng trong hệ thống PTIT-RMS thay đổi trạng thái và tương tác với nhau theo thời gian, bao gồm:

- Biểu đồ trạng thái (State Diagram) cho các đối tượng nghiệp vụ quan trọng.
- Biểu đồ tuần tự (Sequence Diagram) cho các Use Case chính.

Các mô hình này giúp:

- Nhìn rõ vòng đời của đối tượng theo nghiệp vụ thực tế.
- Làm rõ luồng tương tác giữa các actor và hệ thống.
- Hỗ trợ thiết kế chi tiết, kiểm thử và triển khai sau này.

---

## 4.2. Biểu đồ Trạng thái (State Diagrams)

Phần này mô tả vòng đời của các đối tượng:

- Bàn (Table).
- Đơn hàng (Order).
- Món trong Đơn hàng (Order Item).
- Nguyên liệu (Ingredient).

Mỗi vòng đời sẽ được thể hiện bằng một biểu đồ trạng thái và phần mô tả đi kèm.

> 📌 _Ghi chú chèn hình Figma:_  
> Ở mỗi mục dưới đây, bạn chèn link hình state diagram vẽ bằng Figma hoặc PlantUML, theo dạng:  
> `![Hình 4.x – Tên biểu đồ](LINK_HINH_FIGMA_TAI_DAY)`

---

### 4.2.1. Biểu đồ trạng thái vòng đời Bàn (Table State Lifecycle Diagram)

**Các trạng thái chính:**

- **AVAILABLE** – Bàn trống, đã được dọn xong và sẵn sàng nhận khách  
  (Trạng thái mặc định khi vừa cấu hình bàn xong hoặc sau khi dọn xong).

- **RESERVED** – Bàn đã được đặt trước cho một khách hoặc nhóm khách trong một khoảng thời gian cụ thể  
  (Bàn không được gán cho khách khác trong thời gian này trừ khi hủy đặt).

- **SERVING** – Bàn đang được phục vụ  
  (Đã có khách ngồi, có Đơn hàng mở gắn với bàn; khách có thể gọi món, ăn uống, yêu cầu thêm dịch vụ).

- **NEED_CLEAN** – Bàn vừa kết thúc phục vụ, khách đã rời đi, đang chờ nhân viên dọn  
  (Đơn hàng đã được thanh toán nhưng bàn chưa được dọn để phục vụ lượt khách tiếp theo).

- **LOCKED** – Bàn bị khóa, tạm thời không sử dụng để phục vụ khách  
  (Có thể do bảo trì, hỏng hóc, hoặc dành riêng cho mục đích đặc biệt theo quyết định của Quản lý).

> Trạng thái kỹ thuật `[Start]` và `[End]` trong biểu đồ chỉ đại diện cho điểm bắt đầu và kết thúc vòng đời, không phải trạng thái nghiệp vụ.

**Các chuyển trạng thái chính:**

1. **Khởi tạo hoặc cấu hình bàn mới**

   - `[Start] → AVAILABLE`
   - Khi bàn được tạo mới trong hệ thống hoặc sau lần dọn đầu tiên, bàn sẵn sàng sử dụng.

2. **Đặt bàn trước**

   - `AVAILABLE → RESERVED`
   - Khi nhân viên hoặc Quản lý tạo một yêu cầu đặt bàn thành công cho khách theo ngày, giờ, số khách.

3. **Hủy đặt bàn**

   - `RESERVED → AVAILABLE`
   - Khi đặt bàn bị hủy (khách báo hủy, hết thời gian giữ bàn mà khách không đến, hoặc Quản lý quyết định hủy).

4. **Khách vãng lai đến và ngồi trực tiếp**

   - `AVAILABLE → SERVING`
   - Khi khách không đặt trước nhưng đến trực tiếp, Nhân viên phục vụ gán khách vào bàn và mở Đơn hàng mới.

5. **Khách đặt trước đến nhận bàn**

   - `RESERVED → SERVING`
   - Khi khách đã đặt bàn trước đến đúng giờ, Nhân viên phục vụ xác nhận và chuyển bàn sang trạng thái “Đang phục vụ”.

6. **Kết thúc phục vụ, chuyển sang chờ dọn**

   - `SERVING → NEED_CLEAN`
   - Sau khi Đơn hàng đã thanh toán xong, Nhân viên phục vụ kết thúc phục vụ bàn; bàn chuyển sang trạng thái “Cần dọn”.

7. **Dọn bàn xong, quay lại Trống**

   - `NEED_CLEAN → AVAILABLE`
   - Khi Nhân viên phục vụ dọn xong bàn và xác nhận trên hệ thống, bàn lại sẵn sàng để nhận khách mới.

8. **Khóa bàn (chỉ Quản lý)**

   - `AVAILABLE → LOCKED`
     - Khi Quản lý quyết định khóa bàn (bảo trì, hư hỏng, không sử dụng tạm thời).
   - `RESERVED → LOCKED`
     - Trường hợp đặc biệt, Quản lý vừa hủy đặt vừa khóa bàn luôn (bàn gặp sự cố ngay trước giờ khách đến).

9. **Mở khóa bàn**

   - `LOCKED → AVAILABLE`
   - Khi bàn đã được bảo trì xong, Quản lý mở khóa để bàn trở lại trạng thái có thể phục vụ.

10. **Kết thúc vòng đời (xóa khỏi hệ thống)**

    - `AVAILABLE → [End]`
    - `LOCKED → [End]`
    - Mô tả trường hợp hiếm: khi bàn bị xóa khỏi cấu hình hệ thống (nhà hàng thay đổi mặt bằng, bỏ hẳn bàn đó).

> 📌 _Hình 4.1 – Biểu đồ trạng thái vòng đời Bàn_  
> `![Hình 4.1 – Biểu đồ trạng thái vòng đời Bàn](LINK_HINH_STATE_TABLE)`

---

### 4.2.2. Biểu đồ trạng thái vòng đời Đơn hàng (Order State Lifecycle Diagram)

**Các trạng thái chính:**

- **OPEN** – Đơn vừa được tạo, bắt đầu ghi món  
  (Thường khi Nhân viên phục vụ mở đơn cho bàn vừa nhận khách).

- **SERVING** – Đơn đang được phục vụ  
  (Món đang chế biến, khách gọi thêm món, trạng thái tồn tại trong suốt bữa ăn).

- **CLOSED** – Đơn đã thanh toán xong  
  (Hóa đơn đã tạo, tiền đã nhận, đơn dùng cho đối soát và báo cáo).

- **CANCELLED** – Đơn đã hủy  
  (Đơn không tiếp tục sử dụng nữa, thông tin vẫn lưu để tra cứu lịch sử).

**Các chuyển trạng thái chính:**

1. **Mở đơn hàng mới**

   - `[Start] → OPEN`
   - Khi Nhân viên phục vụ mở đơn hàng cho một bàn: ghi nhận ca phục vụ, người mở đơn, thời gian bắt đầu.

2. **Bắt đầu phục vụ – gửi món cho bếp**

   - `OPEN → SERVING`
   - Khi món đầu tiên được gửi sang bếp, đơn chuyển sang trạng thái “Đang phục vụ”.

3. **Hoàn tất thanh toán**

   - `SERVING → CLOSED`
   - Khi Thu ngân xử lý xong thanh toán và tạo bản ghi Hóa đơn, đơn hàng chuyển sang “Đã thanh toán”.

4. **Hủy đơn trước khi phục vụ**

   - `OPEN → CANCELLED`
   - Khi khách đổi ý hoặc nhập nhầm, đơn chưa có món hoặc chưa gửi món cho bếp.

5. **Hủy đơn trong khi đang phục vụ**

   - `SERVING → CANCELLED`
   - Trường hợp đặc biệt theo quyền Quản lý (sự cố hệ thống, nhập nhầm cho sai bàn…)
   - Thường cần log chi tiết lý do theo RB-07.

6. **Kết thúc vòng đời**

   - `CLOSED → [End]`
   - `CANCELLED → [End]`
   - Đơn hàng không còn thay đổi trạng thái thêm, thông tin chỉ dùng cho báo cáo và tra cứu.

> 📌 _Hình 4.2 – Biểu đồ trạng thái vòng đời Đơn hàng_  
> `![Hình 4.2 – Biểu đồ trạng thái vòng đời Đơn hàng](LINK_HINH_STATE_ORDER)`

---

### 4.2.3. Biểu đồ trạng thái vòng đời Món trong Đơn hàng (Order Item Lifecycle)

**Các trạng thái chính:**

- **ORDERED** – Món đã được gọi và ghi nhận vào Đơn hàng, đang chờ bếp tiếp nhận  
  (Trạng thái sau khi Nhân viên phục vụ bấm “Gửi bếp”).

- **IN_KITCHEN** – Món đang trong quá trình chế biến tại bếp  
  (Đầu bếp bấm “Bắt đầu chế biến”, món được ưu tiên xử lý).

- **READY** – Món đã được chế biến xong, đang chờ Nhân viên phục vụ mang ra cho khách  
  (Đầu bếp bấm “Hoàn thành món”, món sẵn sàng phục vụ).

- **SERVED** – Món đã được phục vụ cho khách tại bàn  
  (Nhân viên phục vụ xác nhận đã mang món ra, món không còn nằm trong hàng chờ).

- **CANCELLED** – Món bị hủy, không tiếp tục chế biến hoặc phục vụ  
  (Do khách đổi món, hết nguyên liệu, món bị lỗi, hoặc quyết định khác theo nghiệp vụ).

**Các chuyển trạng thái chính:**

1. **Gửi món cho bếp**

   - `[Start] → ORDERED`
   - Khi Nhân viên phục vụ thêm món vào đơn và bấm “Gửi bếp”.

2. **Bắt đầu chế biến**

   - `ORDERED → IN_KITCHEN`
   - Bếp nhấn “Bắt đầu chế biến”.

3. **Hoàn thành món**

   - `IN_KITCHEN → READY`
   - Bếp nhấn “Hoàn thành món”.

4. **Phục vụ món cho khách**

   - `READY → SERVED`
   - Nhân viên phục vụ mang món ra bàn, xác nhận món đã phục vụ trên hệ thống.

5. **Hủy món khi đang chờ bếp**

   - `ORDERED → CANCELLED`
   - Món vẫn ở trạng thái mới gọi, bếp chưa bắt đầu chế biến, khách đổi ý hoặc đổi món khác.

6. **Hủy món khi đang chế biến hoặc sau khi làm xong nhưng chưa phục vụ**

   - `IN_KITCHEN → CANCELLED`
     - Hủy món khi đang chế biến, chỉ cho phép trong một số trường hợp đặc biệt và đúng quyền.
   - `READY → CANCELLED`
     - Hủy món đã nấu xong nhưng chưa mang ra bàn (món bị lỗi trình bày, khách từ chối món, v.v.).

7. **Kết thúc vòng đời món**

   - `SERVED → [End]`
     - Món đã được phục vụ và bữa ăn kết thúc.
   - `CANCELLED → [End]`
     - Món bị hủy, thông tin vẫn lưu để báo cáo và kiểm tra.

> 📌 _Hình 4.3 – Biểu đồ trạng thái vòng đời Món trong Đơn hàng_  
> `![Hình 4.3 – Biểu đồ trạng thái vòng đời Món trong Đơn hàng](LINK_HINH_STATE_ORDER_ITEM)`

---

### 4.2.4. Biểu đồ trạng thái vòng đời Nguyên liệu (Ingredient State Lifecycle)

**Các trạng thái chính:**

- **ACTIVE** – Nguyên liệu đang sử dụng bình thường  
  (Có tồn kho, được phép dùng để chế biến món).

- **LOW_STOCK** – Nguyên liệu có tồn kho dưới ngưỡng an toàn  
  (Vẫn còn hàng nhưng ở mức thấp, cần nhập thêm sớm).

- **OUT_OF_STOCK** – Nguyên liệu hết hàng  
  (Tồn kho bằng 0, không thể tiếp tục sử dụng cho món).

- **INACTIVE** – Nguyên liệu ngừng sử dụng trong hệ thống  
  (Không còn dùng trong thực đơn hiện tại hoặc bị loại bỏ vĩnh viễn).

**Các chuyển trạng thái chính:**

1. **Khởi tạo nguyên liệu mới**

   - `[Start] → ACTIVE`
   - Khi Quản lý tạo mới một nguyên liệu và nhập tồn kho ban đầu ở mức bình thường.

2. **Biến động tồn kho dẫn đến Low Stock**

   - `ACTIVE → LOW_STOCK`
     - Khi xuất kho hoặc tiêu thụ nguyên liệu làm tồn kho giảm xuống dưới ngưỡng an toàn.
   - `LOW_STOCK → ACTIVE`
     - Khi nhập kho, tồn kho tăng lên bằng hoặc vượt ngưỡng an toàn.

3. **Biến động dẫn đến hết hàng**

   - `LOW_STOCK → OUT_OF_STOCK`
     - Khi tiếp tục xuất kho phần tồn còn lại, tồn kho giảm về 0.
   - `ACTIVE → OUT_OF_STOCK`
     - Trường hợp đặc biệt: điều chỉnh tồn làm từ mức bình thường về 0 trong một lần (ví dụ kiểm kê phát hiện thiếu).

4. **Nhập kho lại sau khi hết hàng**

   - `OUT_OF_STOCK → LOW_STOCK`
     - Khi có nhập kho nhưng số lượng nhập vẫn chưa đủ để vượt ngưỡng an toàn.
   - `OUT_OF_STOCK → ACTIVE`
     - Khi có nhập kho với số lượng đủ lớn, tồn vượt ngưỡng an toàn.

5. **Ngừng sử dụng nguyên liệu**

   - `ACTIVE → INACTIVE`
   - `LOW_STOCK → INACTIVE`
   - `OUT_OF_STOCK → INACTIVE`
   - Khi Quản lý quyết định ngừng sử dụng nguyên liệu đó trong thực đơn hoặc không nhập nữa.

6. **Kích hoạt lại nguyên liệu**

   - `INACTIVE → ACTIVE`
   - Khi Quản lý khôi phục nguyên liệu để dùng trở lại.

7. **Kết thúc vòng đời (xóa khỏi hệ thống)**

   - `INACTIVE → [End]`
   - Khi nguyên liệu bị xóa vĩnh viễn khỏi hệ thống, không còn xuất hiện trong bất kỳ màn hình nghiệp vụ nào.

> 📌 _Hình 4.4 – Biểu đồ trạng thái vòng đời Nguyên liệu_  
> `![Hình 4.4 – Biểu đồ trạng thái vòng đời Nguyên liệu](LINK_HINH_STATE_INGREDIENT)`

---

## 4.3. Biểu đồ Tuần tự (Sequence Diagrams)

Phần này mô tả các **Sequence Diagram** tương ứng với những Use Case trọng tâm đã mô tả ở **SRS - Phần 2**, bao gồm:

- UC-01: Tiếp nhận khách và gán bàn.
- UC-02: Gọi món và quản lý đơn hàng tại bàn.
- UC-03: Xử lý chế biến món tại bếp.
- UC-04: Thanh toán và in hóa đơn.

Mỗi biểu đồ sẽ cho thấy:

- Các đối tượng tham gia (actors và thành phần hệ thống).
- Thứ tự các thông điệp (messages) được gửi nhận theo thời gian.

> 📌 _Ghi chú chèn hình Figma:_  
> Bạn chèn link từng sequence diagram vẽ trên Figma theo format:  
> `![Hình 4.x – Biểu đồ tuần tự UC-0x](LINK_HINH_SEQUENCE_UC_0x)`

---

### 4.3.1. Sequence Diagram cho UC-01 – Tiếp nhận khách & gán bàn

**Đối tượng tham gia (lifelines gợi ý):**

- `Customer` (Khách hàng, có thể chỉ gián tiếp).
- `WaiterUI` (Giao diện Nhân viên phục vụ).
- `TableService` (Service quản lý bàn).
- `ReservationService` (Service quản lý đặt bàn, nếu có).
- `Database` (CSDL).

**Luồng chính tóm tắt:**

1. Customer đến nhà hàng và yêu cầu chỗ ngồi.
2. WaiterUI hiển thị sơ đồ bàn và trạng thái.
3. WaiterUI gửi yêu cầu đến TableService để lấy danh sách bàn trống và bàn đặt trước.
4. TableService truy vấn Database rồi trả về danh sách bàn.
5. Nhân viên chọn bàn phù hợp trên WaiterUI.
6. Nếu bàn có đặt trước, WaiterUI gọi ReservationService để kiểm tra Reservation.
7. Sau khi xác nhận, WaiterUI gửi yêu cầu “Nhận khách” đến TableService.
8. TableService cập nhật trạng thái bàn sang `SERVING` trong Database.
9. WaiterUI hiển thị kết quả và sẵn sàng cho UC-02.

> 📌 _Hình 4.5 – Biểu đồ tuần tự UC-01: Tiếp nhận khách & gán bàn_  
> `![Hình 4.5 – Biểu đồ tuần tự UC-01](LINK_HINH_SEQUENCE_UC01)`

---

### 4.3.2. Sequence Diagram cho UC-02 – Gọi món & quản lý đơn hàng tại bàn

**Đối tượng tham gia gợi ý:**

- `WaiterUI`
- `OrderService`
- `MenuService`
- `InventoryService`
- `KitchenQueue` hoặc `KitchenDisplay`
- `Database`

**Luồng chính tóm tắt:**

1. WaiterUI hiển thị thông tin bàn và đơn hiện tại (nếu có).
2. Nhân viên tìm món thông qua MenuService, MenuService đọc dữ liệu từ Database.
3. Khi nhân viên chọn món và nhấn “Thêm vào đơn”, WaiterUI gọi OrderService tạo hoặc cập nhật `Order` và `OrderDetail`.
4. OrderService yêu cầu InventoryService kiểm tra tồn kho theo định lượng.
5. InventoryService đọc `Ingredient` và `MenuItemIngredient` từ Database, phản hồi đủ hoặc thiếu.
6. Nếu đủ, OrderService lưu chi tiết đơn, cập nhật tạm tính và gửi thông tin món mới tới `KitchenQueue` hoặc `KitchenDisplay`.
7. KitchenDisplay cập nhật danh sách món “Chờ chế biến” cho bếp.
8. WaiterUI hiển thị lại danh sách món, tạm tính và trạng thái gửi bếp.

> 📌 _Hình 4.6 – Biểu đồ tuần tự UC-02: Gọi món & quản lý đơn hàng tại bàn_  
> `![Hình 4.6 – Biểu đồ tuần tự UC-02](LINK_HINH_SEQUENCE_UC02)`

---

### 4.3.3. Sequence Diagram cho UC-03 – Xử lý chế biến món tại bếp

**Đối tượng tham gia gợi ý:**

- `ChefUI` (Màn hình bếp).
- `KitchenService` hoặc sử dụng lại `OrderService`.
- `Database`.

**Luồng chính tóm tắt:**

1. ChefUI tải danh sách món ở trạng thái `ORDERED` từ KitchenService.
2. KitchenService truy vấn Database (bảng `order_details`) để lấy các món cần chế biến.
3. Khi bếp chọn món và nhấn “Bắt đầu chế biến”, ChefUI gửi yêu cầu cập nhật trạng thái món sang `IN_KITCHEN`.
4. KitchenService cập nhật trạng thái món trong Database.
5. Sau khi chế biến xong, bếp nhấn “Hoàn thành món”, ChefUI gửi yêu cầu cập nhật trạng thái sang `READY`.
6. KitchenService cập nhật Database và có thể gửi thông báo sang WaiterUI để thông báo món đã sẵn sàng.
7. Khi Nhân viên phục vụ xác nhận “Đã phục vụ”, trạng thái món chuyển sang `SERVED` qua OrderService.

> 📌 _Hình 4.7 – Biểu đồ tuần tự UC-03: Xử lý chế biến món tại bếp_  
> `![Hình 4.7 – Biểu đồ tuần tự UC-03](LINK_HINH_SEQUENCE_UC03)`

---

### 4.3.4. Sequence Diagram cho UC-04 – Thanh toán & in hóa đơn

**Đối tượng tham gia gợi ý:**

- `CashierUI`.
- `OrderService`.
- `BillingService`.
- `Database`.
- `Printer` hoặc `PDFGenerator`.

**Luồng chính tóm tắt:**

1. CashierUI lấy danh sách bàn/đơn đang chờ thanh toán từ OrderService.
2. OrderService truy vấn Database, trả về danh sách Order ở trạng thái `SERVING` hoặc `READY_FOR_PAYMENT`.
3. Thu ngân chọn một đơn hàng, CashierUI yêu cầu OrderService trả chi tiết món.
4. OrderService trả về danh sách `OrderDetail` để hiển thị trên CashierUI.
5. Thu ngân nhập thông tin giảm giá, chọn phương thức thanh toán, sau đó nhấn “Thanh toán”.
6. CashierUI gửi yêu cầu tới BillingService để tạo `Bill`.
7. BillingService:
   - Kiểm tra lại trạng thái món (tất cả `SERVED` hoặc `CANCELLED` theo RB-05).
   - Tính toán tạm tính, giảm giá, tổng phải trả.
   - Lưu bản ghi `Bill` vào Database.
   - Cập nhật trạng thái `Order` sang `CLOSED` và trạng thái `Table` sang `NEED_CLEAN`.
8. BillingService gửi dữ liệu hóa đơn cho Printer hoặc PDFGenerator để in hoặc xuất file.
9. CashierUI hiển thị thông báo “Thanh toán thành công”.

> 📌 _Hình 4.8 – Biểu đồ tuần tự UC-04: Thanh toán & in hóa đơn_  
> `![Hình 4.8 – Biểu đồ tuần tự UC-04](LINK_HINH_SEQUENCE_UC04)`

---

# CHƯƠNG 5. GIAO DIỆN NGƯỜI DÙNG & WIREFRAME

## 5.1. Nguyên tắc thiết kế giao diện

Giao diện người dùng của PTIT-RMS được thiết kế dựa trên các nguyên tắc:

- **Đơn giản, trực quan, nhất quán**  
  Ưu tiên thông tin quan trọng, bố cục rõ ràng cho từng vai trò (phục vụ, bếp, thu ngân, quản lý).

- **Tối ưu thao tác cho nghiệp vụ lặp lại**  
  Giảm số bước click để thực hiện các thao tác thường xuyên như:

  - Chọn bàn và mở đơn.
  - Gọi món, gửi bếp.
  - Thanh toán và in hóa đơn.

- **Sử dụng màu sắc và biểu tượng hợp lý**  
  Gợi ý:

  - Trạng thái bàn: Xanh (Trống), Vàng (Đang phục vụ), Xanh dương (Đặt trước), Xám (Khóa), Cam/Đỏ nhạt (Cần dọn).
  - Trạng thái món ở bếp: Cam (Đang chế biến), Xanh lá (Hoàn thành), Xám (Hủy).

- **Tương thích với màn hình POS/máy tính và cả màn hình cảm ứng**  
  Các nút bấm đủ lớn, khoảng cách hợp lý, dễ thao tác trên môi trường nhà hàng.

> 📌 _Ghi chú:_  
> Toàn bộ wireframe có thể thiết kế trên Figma, mỗi màn hình là một Frame riêng, theo layout mà bạn đã code HTML/CSS demo.

---

## 5.2. Wireframe 1: Màn hình Sơ đồ bàn & Gọi món tại bàn

**Mục đích:**

- Cung cấp cho Nhân viên phục vụ một màn hình duy nhất để:
  - Xem nhanh tình trạng toàn bộ bàn.
  - Chọn bàn, mở hoặc tiếp tục Đơn hàng.
  - Ghi nhận món, gửi món cho bếp.
  - Theo dõi tạm tính cho khách.

**Bố cục gợi ý:**

- **Sidebar trái**

  - Logo, tên hệ thống PTIT-RMS.
  - Navigation: Dashboard, Bàn & Gọi món, Bếp, Thanh toán, Kho, v.v.

- **Khu vực “Sơ đồ bàn” (chiếm phần lớn bên trái)**

  - Grid các “Thẻ bàn” (Table Card): hiển thị:
    - Mã bàn (T01, T02,…).
    - Khu vực, số ghế.
    - Trạng thái bóng màu hoặc tag trạng thái.

- **Khu vực “Đơn hàng tại bàn” (bên phải)**
  - Thông tin đầu: Bàn, số khách, khu vực, trạng thái.
  - Danh sách món hiện tại (OrderDetail): tên món, SL, đơn giá, thành tiền, trạng thái món.
  - Khu vực tạm tính, giảm giá, ghi chú.
  - Bộ nút hành động:
    - Thêm món, Gửi bếp, Yêu cầu thanh toán, Lưu.

> 📌 _Hình 5.1 – Wireframe màn hình Sơ đồ bàn & Gọi món tại bàn_  
> `![Hình 5.1 – Wireframe Sơ đồ bàn & Gọi món](LINK_HINH_WIREFRAME_TABLE_ORDER)`

---

## 5.3. Wireframe 2: Màn hình Bếp xử lý món

**Mục đích:**

- Tối ưu hoá thao tác cho Nhân viên bếp: thấy ngay món nào cần làm, đang làm, đã xong.

**Bố cục gợi ý:**

- **Ba cột chính:**

  - Cột 1: “Chờ chế biến”
  - Cột 2: “Đang chế biến”
  - Cột 3: “Đã sẵn sàng”

- Mỗi “ticket món” hiển thị:

  - Tên món, số lượng.
  - Bàn tương ứng, ghi chú yêu cầu.
  - Thời điểm gửi món vào bếp.
  - Các nút: “Bắt đầu chế biến”, “Hoàn thành”, “Không thể chế biến”.

- Màn hình bếp có thể auto-refresh hoặc nhận push khi có món mới.

> 📌 _Hình 5.2 – Wireframe màn hình Bếp xử lý món_  
> `![Hình 5.2 – Wireframe Bếp xử lý món](LINK_HINH_WIREFRAME_KITCHEN)`

---

## 5.4. Wireframe 3: Màn hình Thanh toán & Dashboard Quản lý

Bạn có thể tách thành 2 view riêng:

### 5.4.1. Màn hình Thanh toán

**Bố cục gợi ý:**

- **Panel trái:** danh sách bàn/đơn đang chờ thanh toán.
- **Panel phải:** chi tiết đơn, tạm tính, giảm giá, tổng tiền, lựa chọn phương thức thanh toán, nút in hóa đơn.

> 📌 _Hình 5.3 – Wireframe màn hình Thanh toán_  
> `![Hình 5.3 – Wireframe Thanh toán](LINK_HINH_WIREFRAME_PAYMENT)`

### 5.4.2. Dashboard Quản lý

**Nội dung gợi ý:**

- Các thẻ KPI: tổng doanh thu hôm nay, số bàn đã phục vụ, số đơn hủy, số nguyên liệu dưới ngưỡng.
- Biểu đồ doanh thu theo khung giờ; cơ cấu doanh thu theo nhóm món.
- Danh sách nhanh:
  - Top món bán chạy.
  - Nguyên liệu dưới ngưỡng cảnh báo.

> 📌 _Hình 5.4 – Wireframe Dashboard Quản lý_  
> `![Hình 5.4 – Wireframe Dashboard Quản lý](LINK_HINH_WIREFRAME_DASHBOARD)`

---

# PHỤ LỤC (APPENDIX)

## A. Biểu đồ Lớp (Class Diagram – Domain Model)

**Các lớp chính:**

- `Table`
- `Order`
- `OrderDetail`
- `MenuItem`
- `Ingredient`
- `MenuItemIngredient` (bảng trung gian MenuItem – Ingredient)
- `Bill`
- `User` (đại diện cho Staff, Manager, Chef, Cashier, Waiter)

**Quan hệ chính:**

- `Table 1–N Order`  
  Một bàn có thể có nhiều đơn hàng theo thời gian (nhiều lượt khách khác nhau).  
  Trong nghiệp vụ hiện tại, tại một thời điểm chỉ tồn tại tối đa 1 `Order` đang phục vụ cho một `Table` (RB-01).

- `Order 1–N OrderDetail`  
  Một `Order` có nhiều dòng món, mỗi dòng là một `OrderDetail`.

- `MenuItem N–N Ingredient` thông qua `MenuItemIngredient`  
  Một món có thể dùng nhiều nguyên liệu.  
  Một nguyên liệu có thể dùng cho nhiều món.

- `Order 1–1 Bill`  
  Mỗi đơn hàng sau khi thanh toán tạo ra đúng một `Bill` tương ứng.

- `User 1–N Order` và `User 1–N Bill`  
  Phục vụ tạo đơn, Thu ngân tạo hóa đơn, Quản lý tạo hoặc chỉnh sửa menu và kho.

**Gợi ý triển khai trên Class Diagram:**

- Sử dụng các association với multiplicity:
  - `Table "1" --- "0..*" Order`
  - `Order "1" --- "1..*" OrderDetail`
  - `MenuItem "1" --- "0..*" MenuItemIngredient "0..*" --- "1" Ingredient`
  - `Order "1" --- "0..1" Bill`
- Thêm các thuộc tính cơ bản:
  - `Table`: code, area, capacity, status
  - `Order`: code, openedAt, closedAt, status
  - `OrderDetail`: quantity, unitPrice, totalPrice, itemStatus
  - `MenuItem`: name, category, price, status
  - `Ingredient`: name, unit, stockQuantity, minThreshold, status
  - `Bill`: totalAmount, discountPct, discountValue, finalAmount, paymentMethod, paidAt
  - `User`: name, email, password, role

> 📌 _Hình A.1 – Biểu đồ Lớp (Domain Model) PTIT-RMS_  
> `![Hình A.1 – Biểu đồ Lớp (Domain Model) PTIT-RMS](LINK_HINH_CLASS_DIAGRAM)`

---

## B. Liên kết Figma và tài nguyên thiết kế (tuỳ chọn)

Bạn có thể liệt kê tại đây các link Figma và tài nguyên thiết kế tương ứng:

- **Figma – State Diagrams:**  
  `LINK_FIGMA_STATE_DIAGRAMS`

- **Figma – Sequence Diagrams:**  
  `LINK_FIGMA_SEQUENCE_DIAGRAMS`

- **Figma – Wireframe & UI Kit:**  
  `LINK_FIGMA_WIREFRAME_UI`

- **Prototype tương tác (nếu có):**  
  `LINK_FIGMA_PROTOTYPE`

---

> 📌 _Kết thúc SRS - Phần 3_  
> Bạn có thể ghép 3 file: `SRS - Phần 1`, `SRS - Phần 2`, `SRS - Phần 3` vào chung một file Word/Docs để tạo bản SRS hoàn chỉnh, sau đó lần lượt chèn hình từ Figma, ERD từ DBMS, và cập nhật số hình (Hình 2.x, 3.x, 4.x…) theo đúng thứ tự trong tài liệu.
