<!-- File: SRS - Phần 2 -->

# CHƯƠNG 3. YÊU CẦU CỤ THỂ (SPECIFIC REQUIREMENTS)

## 3.1. Danh sách Use Case (Use Case List)

Bảng dưới đây liệt kê các Use Case chính của hệ thống PTIT-RMS, gắn với các actor đã mô tả ở Chương 2.

| Mã UC | Tên Use Case                                    | Mô tả ngắn                                                                                | Actor chính                |
| ----- | ----------------------------------------------- | ----------------------------------------------------------------------------------------- | -------------------------- |
| UC-01 | Tiếp nhận khách và gán bàn                      | Nhân viên phục vụ tiếp nhận khách, chọn bàn phù hợp và cập nhật trạng thái bàn.           | Nhân viên phục vụ (Waiter) |
| UC-02 | Gọi món và quản lý đơn hàng tại bàn             | Ghi nhận món cho bàn (thêm/sửa/xóa món), gửi món cho bếp, theo dõi trạng thái đơn.        | Nhân viên phục vụ (Waiter) |
| UC-03 | Xử lý chế biến món tại bếp                      | Bếp nhận danh sách món cần chế biến, cập nhật trạng thái món (Đang chế biến, Hoàn thành). | Nhân viên bếp (Chef)       |
| UC-04 | Thanh toán và in hóa đơn                        | Thu ngân xem đơn, áp dụng giảm giá, xử lý thanh toán và in hóa đơn.                       | Thu ngân (Cashier)         |
| UC-05 | Quản lý thực đơn (Menu Management)              | Quản lý danh sách món ăn/đồ uống, danh mục, giá bán, trạng thái kinh doanh.               | Quản lý (Manager)          |
| UC-06 | Quản lý kho nguyên liệu & cảnh báo tồn kho      | Quản lý nhập/xuất tồn kho, xem báo cáo tồn, xử lý các cảnh báo nguyên liệu thấp.          | Quản lý (Manager)          |
| UC-07 | Xem báo cáo (Report & Dashboard)                | Xem tổng quan doanh thu, bàn phục vụ, nguyên liệu dưới ngưỡng,…                           | Quản lý (Manager)          |
| UC-08 | Đăng nhập & phân quyền (Authentication & Roles) | Người dùng đăng nhập vào hệ thống; hệ thống phân quyền theo vai trò.                      | Tất cả user nội bộ         |

> Ghi chú: Danh sách Use Case có thể được mở rộng/chi tiết thêm trong quá trình thiết kế, nhưng các UC trên là **lõi nghiệp vụ** tương ứng với sơ đồ Use Case tổng thể (Hình 2.2).

---

## 3.2. Mô tả chi tiết Use Case (Use Case Descriptions)

Phần này mô tả chi tiết một số Use Case **trọng tâm** của hệ thống, theo mẫu:

- Mã & Tên Use Case
- Actor
- Mô tả
- Điều kiện tiên quyết (Preconditions)
- Kết quả sau cùng (Postconditions)
- Luồng chính (Main Flow)
- Luồng thay thế / ngoại lệ (Alternative / Exception Flows)

### 3.2.1. UC-01 – Tiếp nhận khách và gán bàn

**Mã UC:** UC-01  
**Tên:** Tiếp nhận khách và gán bàn (Receive Customer & Assign Table)  
**Actor chính:** Nhân viên phục vụ (Waiter)  
**Mô tả:**  
Khi khách đến nhà hàng, nhân viên phục vụ kiểm tra bàn trống hoặc bàn đặt trước, sau đó gán khách vào bàn phù hợp và cập nhật trạng thái bàn trong hệ thống.

**Preconditions:**

- Nhân viên đã **đăng nhập thành công** (UC-08).
- Sơ đồ bàn (khu vực, số ghế, trạng thái) đã được cấu hình sẵn bởi Quản lý.
- Nếu là khách đặt trước, thông tin Reservation đã được nhập trước đó.

**Postconditions:**

- Bàn được chọn chuyển sang trạng thái **Đang phục vụ**.
- Nếu là khách đặt trước:
  - Trạng thái đặt bàn được cập nhật sang “Đã nhận bàn”.
- Thông tin cơ bản về khách (nếu có) được liên kết với lượt phục vụ.

**Luồng chính (Main Flow):**

1. Nhân viên mở màn hình **Sơ đồ bàn**.
2. Hệ thống hiển thị danh sách bàn theo khu vực và trạng thái (Trống, Đang phục vụ, Đặt trước,…).
3. Nhân viên hỏi số lượng khách, kiểm tra khu vực/bàn phù hợp.
4. Nhân viên:
   - Chọn một bàn **Trống**, hoặc
   - Chọn một bàn ở trạng thái **Đặt trước** nếu khách đã đặt từ trước.
5. Hệ thống hiển thị popup/khung thông tin:
   - Mã bàn, khu vực, sức chứa.
   - Thông tin đặt trước (nếu có): tên khách, số điện thoại, thời gian đặt.
6. Nhân viên xác nhận **“Nhận khách”** cho bàn đó.
7. Hệ thống:
   - Cập nhật trạng thái bàn → **Đang phục vụ**.
   - Nếu là bàn “Đặt trước”, cập nhật trạng thái Reservation → “Đã nhận / Đang sử dụng”.
8. Hệ thống sẵn sàng cho Use Case **UC-02 – Gọi món và quản lý đơn hàng tại bàn**.

**Luồng thay thế / Ngoại lệ:**

- **A1 – Không còn bàn trống phù hợp**

  1. Ở bước 4, nhân viên không tìm được bàn trống có sức chứa phù hợp.
  2. Nhân viên có thể:
     - Đề xuất khách ghép bàn/khu vực khác, hoặc
     - Từ chối nếu khách không chấp nhận.
  3. Hệ thống **không** cập nhật trạng thái bàn nào.

- **A2 – Khách đặt trước đến trễ quá thời gian giữ bàn**
  1. Bàn đang ở trạng thái **Đặt trước**, hệ thống hiển thị cảnh báo: “Quá thời gian giữ bàn”.
  2. Nhân viên có thể:
     - Hủy đặt bàn (Reservation) và dùng cho khách khác, hoặc
     - Xác nhận vẫn giữ bàn cho khách đặt trước (theo chính sách nhà hàng).
  3. Tùy lựa chọn, hệ thống cập nhật lại trạng thái đặt bàn và bàn tương ứng.

---

### 3.2.2. UC-02 – Gọi món và quản lý đơn hàng tại bàn

**Mã UC:** UC-02  
**Tên:** Gọi món và quản lý đơn hàng tại bàn (Table Ordering & Order Management)  
**Actor chính:** Nhân viên phục vụ (Waiter)  
**Mô tả:**  
Nhân viên phục vụ ghi nhận món khách gọi, thêm/sửa/xóa món trong đơn, gửi món cho bếp, theo dõi trạng thái món theo thời gian thực.

**Preconditions:**

- Bàn đã ở trạng thái **Đang phục vụ** (UC-01 đã hoàn tất).
- Nhân viên đã đăng nhập và được phân quyền “Phục vụ”.
- Menu và định lượng món – nguyên liệu đã được Quản lý cấu hình.

**Postconditions:**

- Đơn hàng được tạo/ cập nhật kèm các món, số lượng, giá, ghi chú.
- Các món đã “Gửi bếp” xuất hiện trong màn hình bếp (UC-03).
- Trạng thái đơn hàng được cập nhật tương ứng (Mở → Đang phục vụ → Chờ thanh toán).

**Luồng chính (Main Flow):**

1. Nhân viên chọn một bàn đang phục vụ trên sơ đồ bàn.
2. Hệ thống mở màn hình **Đơn hàng tại bàn**, hiển thị:
   - Danh sách món đã được gọi (nếu có).
   - Tạm tính, ghi chú đơn.
3. Nhân viên tìm món trong menu (theo tên / danh mục).
4. Nhân viên chọn món, nhập:
   - Số lượng.
   - Ghi chú đặc biệt (độ cay, không hành,… nếu có).
5. Nhân viên nhấn **“Thêm vào đơn”**, hệ thống:
   - Thêm dòng món vào đơn hàng.
   - Tạm tính lại tổng tiền.
6. Khi khách đã gọi xong một đợt, nhân viên nhấn **“Gửi bếp”** cho các món mới.
7. Hệ thống:
   - Kiểm tra tồn kho (RB-02).
   - Nếu hợp lệ, tạo các thông tin Order Item ở trạng thái “Chờ chế biến” và hiển thị bên màn hình bếp.
8. Nhân viên có thể lặp lại các bước 3–7 khi khách gọi thêm món trong quá trình ăn.
9. Khi khách yêu cầu thanh toán, nhân viên nhấn **“Yêu cầu thanh toán”**, hệ thống chuyển sang luồng UC-04.

**Luồng thay thế / Ngoại lệ:**

- **B1 – Thiếu tồn kho nguyên liệu (vi phạm RB-02)**

  1. Ở bước 7, hệ thống phát hiện một hoặc nhiều món không đủ nguyên liệu.
  2. Hệ thống hiển thị thông báo:
     - Món nào bị ảnh hưởng.
     - Gợi ý: “Thông báo khách đổi món khác” hoặc “Chỉ gửi các món còn đủ nguyên liệu”.
  3. Nhân viên trao đổi với khách, sau đó:
     - Xóa món, hoặc
     - Giảm số lượng, hoặc
     - Đổi món.
  4. Hệ thống cập nhật lại đơn hàng và tạm tính.

- **B2 – Khách muốn hủy/giảm món đã gửi bếp**
  1. Ở bước sau khi gửi bếp, khách muốn hủy/giảm một món.
  2. Hệ thống kiểm tra trạng thái món:
     - Nếu món còn “Chờ chế biến” → cho phép hủy/giảm, cập nhật lại tồn kho.
     - Nếu món đã “Đang chế biến” hoặc “Đã hoàn thành” → chỉ cho phép hủy theo quyền và chính sách của nhà hàng (có thể log lại, không trừ nguyên liệu…).

---

### 3.2.3. UC-03 – Xử lý chế biến món tại bếp

**Mã UC:** UC-03  
**Tên:** Xử lý chế biến món tại bếp (Kitchen Processing)  
**Actor chính:** Nhân viên bếp (Chef)  
**Mô tả:**  
Bếp nhận danh sách món cần chế biến từ các bàn, cập nhật trạng thái món theo từng bước: Chờ chế biến → Đang chế biến → Hoàn thành → Đã phục vụ.

**Preconditions:**

- Đã có món được gửi từ UC-02.
- Nhân viên bếp đăng nhập vào màn hình bếp.

**Postconditions:**

- Trạng thái các món được cập nhật trong hệ thống.
- Nhân viên phục vụ biết món nào đã sẵn sàng để mang ra cho khách.

**Luồng chính (Main Flow):**

1. Nhân viên bếp mở màn hình **Bếp**.
2. Hệ thống hiển thị:
   - Cột “Chờ chế biến”.
   - Cột “Đang chế biến”.
   - Cột “Đã sẵn sàng”.
3. Khi bắt đầu làm một món, bếp nhấn **“Bắt đầu chế biến”**:
   - Trạng thái món chuyển từ “Chờ chế biến” → “Đang chế biến”.
4. Sau khi làm xong, bếp nhấn **“Hoàn thành”**:
   - Trạng thái món chuyển từ “Đang chế biến” → “Đã sẵn sàng”.
5. Nhân viên phục vụ nhìn thấy trên màn hình của mình món nào đã “Đã sẵn sàng” để mang ra cho khách.
6. Khi nhân viên phục vụ xác nhận đã mang món ra bàn, trạng thái món chuyển sang **“Đã phục vụ”** (RB-05).

**Luồng thay thế / Ngoại lệ:**

- **C1 – Bếp từ chối món / không thể chế biến**
  1. Vì lý do nguyên liệu hỏng, thiết bị có sự cố…, bếp không thể chế biến món.
  2. Bếp chọn món và nhấn “Không thể chế biến / Yêu cầu đổi món”.
  3. Hệ thống cập nhật trạng thái món và gửi thông báo cho nhân viên phục vụ.
  4. Nhân viên phục vụ trao đổi với khách để chọn phương án thay thế (đổi món, hủy món…).

---

### 3.2.4. UC-04 – Thanh toán và in hóa đơn

**Mã UC:** UC-04  
**Tên:** Thanh toán và in hóa đơn (Payment & Billing)  
**Actor chính:** Thu ngân (Cashier)  
**Mô tả:**  
Thu ngân xử lý thanh toán cho đơn hàng: kiểm tra món, áp dụng giảm giá, nhận tiền từ khách, in hoặc xuất hóa đơn.

**Preconditions:**

- Đơn hàng ở trạng thái **Chờ thanh toán** (các món đã “Đã phục vụ” hoặc “Hủy” – theo RB-05).
- Thu ngân đăng nhập và có quyền truy cập chức năng thanh toán.

**Postconditions:**

- Hóa đơn được tạo ở trạng thái **Đã thanh toán**.
- Đơn hàng chuyển sang trạng thái **Đã đóng / Đã thanh toán**.
- Bàn được chuyển về trạng thái **Cần dọn** (để chuẩn bị phục vụ lượt khách tiếp theo).

**Luồng chính (Main Flow):**

1. Thu ngân mở màn hình **Thanh toán**.
2. Hệ thống hiển thị danh sách:
   - Bàn/Đơn hàng đang chờ thanh toán.
3. Thu ngân chọn một bàn (vd: T02), hệ thống hiển thị:
   - Chi tiết món, số lượng, đơn giá, thành tiền, ghi chú.
   - Tạm tính, giảm giá, tổng tiền dự kiến.
4. Thu ngân (nếu cần) nhập:
   - Mức giảm giá (%) hoặc số tiền giảm trực tiếp (theo chính sách).
5. Hệ thống tính toán lại tổng phải trả.
6. Thu ngân chọn phương thức thanh toán (Tiền mặt, Thẻ, Ví điện tử,…).
7. Thu ngân xác nhận **“Thanh toán”**.
8. Hệ thống:
   - Tạo bản ghi **Hóa đơn (Bill)** tương ứng.
   - Cập nhật trạng thái đơn → **Closed**.
   - Cập nhật trạng thái bàn → **Need Clean (Cần dọn)**.
9. Hệ thống in hóa đơn (hoặc xuất file) để đưa cho khách.

**Luồng thay thế / Ngoại lệ:**

- **D1 – Khách muốn chia hóa đơn (Split bill – tuỳ mức độ hỗ trợ)**

  1. Ở bước 3, khách yêu cầu chia hóa đơn theo nhóm hoặc món.
  2. Nếu hệ thống hỗ trợ:
     - Thu ngân chọn món để gán vào hóa đơn 1, hóa đơn 2,…
     - Hệ thống tạo nhiều hóa đơn cho cùng 1 đơn hàng.
  3. Nếu không hỗ trợ:
     - Xử lý theo quy trình thủ công hoặc chính sách hiện tại của nhà hàng.

- **D2 – Thanh toán thất bại (lỗi thẻ, ví điện tử)**
  1. Ở bước 7, phương thức thanh toán không thành công.
  2. Hệ thống:
     - Không tạo hóa đơn.
     - Giữ nguyên trạng thái đơn là “Chờ thanh toán”.
  3. Thu ngân đề nghị khách đổi phương thức thanh toán.

---

### 3.2.5. UC-06 – Quản lý kho nguyên liệu & cảnh báo tồn kho

**Mã UC:** UC-06  
**Tên:** Quản lý kho nguyên liệu & cảnh báo tồn kho (Inventory Management)  
**Actor chính:** Quản lý (Manager)  
**Mô tả:**  
Quản lý xem danh sách nguyên liệu, tồn kho hiện tại, nhập/xuất/điều chỉnh tồn kho và xử lý các cảnh báo nguyên liệu dưới ngưỡng.

**Preconditions:**

- Quản lý đăng nhập với quyền tương ứng.
- Danh sách nguyên liệu đã được khai báo.

**Postconditions:**

- Số lượng tồn kho được cập nhật chính xác sau mỗi giao dịch nhập/xuất/điều chỉnh.
- Các cảnh báo tồn kho thấp được tạo/bỏ khi tồn kho vượt/ngưỡng.

**Luồng chính (Main Flow):**

1. Quản lý mở màn hình **Nguyên liệu & Tồn kho**.
2. Hệ thống hiển thị bảng danh sách:
   - Tên nguyên liệu, đơn vị, tồn hiện tại, ngưỡng tối thiểu, trạng thái (Bình thường, Dưới ngưỡng, Hết hàng).
3. Quản lý chọn một nguyên liệu để xem chi tiết.
4. Hệ thống hiển thị chi tiết:
   - Tồn hiện tại, ngưỡng, lịch sử nhập/xuất gần nhất.
5. Quản lý nhấn:
   - **“Nhập kho”**: nhập số lượng tăng thêm.
   - **“Xuất kho”**: xuất số lượng dùng cho các mục đích khác (kiểm kê, hủy hàng,…).
   - **“Điều chỉnh”**: chỉnh tồn kho khi kiểm đếm thực tế.
6. Hệ thống cập nhật tồn kho, tính lại trạng thái (RB-03):
   - Nếu tồn < ngưỡng → trạng thái “Dưới ngưỡng”.
   - Nếu tồn = 0 → “Hết hàng”.
   - Nếu tồn ≥ ngưỡng → “Bình thường”.
7. Các thay đổi này được ghi log (RB-07).

**Luồng thay thế / Ngoại lệ:**

- **E1 – Nhập/xuất tồn kho với giá trị âm hoặc không hợp lệ**
  1. Quản lý nhập số lượng <= 0 hoặc ký tự không phải số.
  2. Hệ thống thông báo lỗi, yêu cầu nhập lại.
  3. Không cập nhật bất kỳ dữ liệu nào.

---

## 3.3. Yêu cầu Phi chức năng (Non-functional Requirements)

Phần này liệt kê các yêu cầu phi chức năng **cụ thể, đo được**, chia theo nhóm: hiệu năng, bảo mật, khả dụng, khả mở rộng, và khả dụng (usability).

### 3.3.1. Hiệu năng (Performance)

- **NFR-01 – Thời gian phản hồi:**
  - Các thao tác quan trọng ở ca phục vụ (mở bàn, thêm món, gửi bếp, thanh toán) phải có **thời gian phản hồi \< 2 giây** trong điều kiện tải bình thường (≤ 20 user nội bộ đồng thời).
- **NFR-02 – Tải đồng thời:**
  - Hệ thống phải hỗ trợ tối thiểu **30 kết nối người dùng đồng thời** (phục vụ, bếp, thu ngân, quản lý) mà không bị treo hoặc timeout bất thường.

### 3.3.2. Độ tin cậy & An toàn dữ liệu (Reliability & Data Integrity)

- **NFR-03 – Độ bền dữ liệu:**
  - Tất cả giao dịch quan trọng (tạo/hủy đơn hàng, tạo hóa đơn, nhập/xuất kho) phải được lưu trữ trong CSDL với cơ chế **transaction** đảm bảo **“all-or-nothing”** (không ghi nhận một phần).
- **NFR-04 – Sao lưu dữ liệu:**
  - Hệ thống phải cho phép **sao lưu dữ liệu ít nhất 1 lần/ngày**, và hỗ trợ khôi phục dữ liệu đến thời điểm bất kỳ trong vòng tối thiểu 7 ngày gần nhất.

### 3.3.3. Bảo mật & Phân quyền (Security)

- **NFR-05 – Xác thực & phân quyền:**
  - Tất cả người dùng nội bộ phải đăng nhập bằng tài khoản riêng.
  - Các chức năng quản lý giá, báo cáo doanh thu, điều chỉnh tồn kho **chỉ** hiển thị cho người dùng có vai trò Quản lý.
- **NFR-06 – Ghi log truy cập & thao tác nhạy cảm:**
  - Hệ thống phải lưu log:
    - Đăng nhập/đăng xuất.
    - Tạo/hủy đơn hàng, tạo/hủy hóa đơn.
    - Thay đổi giá món, điều chỉnh tồn kho.
  - Log phải ghi nhận: user, thời gian, thao tác, dữ liệu chính liên quan.

### 3.3.4. Khả dụng & Dễ sử dụng (Availability & Usability)

- **NFR-07 – Khả dụng:**
  - Hệ thống phải sẵn sàng hoạt động trong giờ mở cửa nhà hàng (ví dụ 9h–22h hàng ngày), độ sẵn sàng trong khung giờ này đạt **≥ 99%** (không tính thời gian bảo trì có kế hoạch).
- **NFR-08 – Dễ sử dụng:**
  - Giao diện cho Nhân viên phục vụ và Bếp:
    - Không quá **3 bước** cho hành động thường xuyên (gọi món, gửi bếp, cập nhật trạng thái món).
    - Icon và màu trạng thái phải nhất quán (Trống/Đang phục vụ/Đã đặt/Cần dọn).

### 3.3.5. Khả mở rộng & Bảo trì (Scalability & Maintainability)

- **NFR-09 – Khả mở rộng:**
  - Kiến trúc hệ thống phải cho phép mở rộng thêm chi nhánh (multi-branch) hoặc thêm kênh đặt món online trong tương lai mà **không cần viết lại toàn bộ**.
- **NFR-10 – Tính mô-đun:**
  - Mã nguồn phải được tổ chức theo các module nghiệp vụ rõ ràng (Bàn, Đơn hàng, Bếp, Kho, Báo cáo), để việc bảo trì, thay đổi logic 1 module không ảnh hưởng trực tiếp tới các module khác.

---

## 3.4. Yêu cầu về Cơ sở dữ liệu Logic

### 3.4.1. Mô tả tổng quan mô hình dữ liệu

Mô hình dữ liệu logic của PTIT-RMS được thiết kế xoay quanh các thực thể chính:

- **Table** – Bàn trong nhà hàng.
- **Order** – Đơn hàng gắn với bàn hoặc khách.
- **OrderDetail** – Chi tiết món trong mỗi đơn hàng.
- **MenuItem** – Món ăn/đồ uống trong thực đơn.
- **Ingredient** – Nguyên liệu trong kho.
- **MenuItemIngredient** – Bảng trung gian thể hiện quan hệ N–N giữa món và nguyên liệu.
- **Bill** – Hóa đơn thanh toán, 1–1 với Order (mỗi đơn hàng có tối đa 1 hóa đơn).
- **User** – Người dùng hệ thống (phục vụ, bếp, thu ngân, quản lý).

Quan hệ chính:

- **Table 1–N Order** (mỗi lượt khách tại bàn tạo ra các đơn hàng tương ứng, nhưng trong nghiệp vụ chuẩn thường là 1 đơn “đang phục vụ” tại một thời điểm).
- **Order 1–N OrderDetail** (một đơn hàng có nhiều dòng món).
- **MenuItem N–N Ingredient** thông qua **MenuItemIngredient** (một món dùng nhiều nguyên liệu, một nguyên liệu dùng cho nhiều món).
- **Order 1–1 Bill** (mỗi đơn hàng được thanh toán bởi 1 hóa đơn).

### 3.4.2. Biểu đồ ERD logic

**Hình 3.1 – Biểu đồ ERD logic hệ thống PTIT-RMS**

> 📌 _Ghi chú chèn hình:_
>
> - Tại đây bạn sẽ dùng đoạn SQL bên dưới để tạo ERD bằng DBMS (MySQL Workbench, Draw.io, dbeaver,…), sau đó chụp hình và chèn:  
>   `![Hình 3.1 – Biểu đồ ERD logic hệ thống PTIT-RMS](LINK_HINH_ERD_TAI_DAY)`

### 3.4.3. SQL tạo cấu trúc bảng (Logical Schema)

Đoạn SQL dưới đây mô tả logic các bảng chính theo ERD. Bạn có thể điều chỉnh kiểu dữ liệu/độ dài cho phù hợp với DBMS thực tế.

```sql
-- Bảng người dùng hệ thống
CREATE TABLE users (
    id           INT AUTO_INCREMENT PRIMARY KEY,
    name         VARCHAR(100) NOT NULL,
    email        VARCHAR(100) NOT NULL UNIQUE,
    password     VARCHAR(255) NOT NULL,
    role         ENUM('WAITER', 'CHEF', 'CASHIER', 'MANAGER') NOT NULL,
    created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at   DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Bảng bàn trong nhà hàng
CREATE TABLE tables (
    id           INT AUTO_INCREMENT PRIMARY KEY,
    code         VARCHAR(20) NOT NULL UNIQUE,     -- Mã bàn: T01, T02...
    area         VARCHAR(50) NOT NULL,            -- Khu vực: Tầng 1, Sân vườn...
    capacity     INT NOT NULL,                    -- Số ghế
    status       ENUM('AVAILABLE', 'RESERVED', 'SERVING', 'NEED_CLEAN', 'LOCKED') NOT NULL DEFAULT 'AVAILABLE',
    description  VARCHAR(255),
    created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at   DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Bảng món ăn/đồ uống
CREATE TABLE menu_items (
    id           INT AUTO_INCREMENT PRIMARY KEY,
    name         VARCHAR(150) NOT NULL,
    category     VARCHAR(100) NOT NULL,           -- Danh mục: Món chính, Đồ uống...
    price        DECIMAL(12,2) NOT NULL,
    status       ENUM('ACTIVE', 'INACTIVE', 'OUT_OF_STOCK') NOT NULL DEFAULT 'ACTIVE',
    description  TEXT,
    created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at   DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Bảng nguyên liệu
CREATE TABLE ingredients (
    id             INT AUTO_INCREMENT PRIMARY KEY,
    name           VARCHAR(150) NOT NULL,
    unit           VARCHAR(20) NOT NULL,          -- Đơn vị: kg, lít, cái...
    stock_quantity DECIMAL(12,3) NOT NULL DEFAULT 0,
    min_threshold  DECIMAL(12,3) NOT NULL DEFAULT 0, -- Ngưỡng cảnh báo
    status         ENUM('ACTIVE', 'LOW_STOCK', 'OUT_OF_STOCK', 'INACTIVE') NOT NULL DEFAULT 'ACTIVE',
    created_at     DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at     DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Bảng trung gian món - nguyên liệu (MenuItem N–N Ingredient)
CREATE TABLE menu_item_ingredients (
    id              INT AUTO_INCREMENT PRIMARY KEY,
    menu_item_id    INT NOT NULL,
    ingredient_id   INT NOT NULL,
    quantity_needed DECIMAL(12,3) NOT NULL,        -- Định lượng dùng cho 1 suất món
    CONSTRAINT fk_mi_ingredient_menu_item
        FOREIGN KEY (menu_item_id) REFERENCES menu_items(id),
    CONSTRAINT fk_mi_ingredient_ingredient
        FOREIGN KEY (ingredient_id) REFERENCES ingredients(id)
);

-- Bảng đơn hàng
CREATE TABLE orders (
    id             INT AUTO_INCREMENT PRIMARY KEY,
    code           VARCHAR(30) NOT NULL UNIQUE,     -- Mã đơn: ORD-2023-0001...
    table_id       INT NOT NULL,                   -- Bàn đang phục vụ
    customer_name  VARCHAR(100),                   -- (tuỳ chọn) tên khách
    customer_phone VARCHAR(20),
    status         ENUM('OPEN', 'SERVING', 'CLOSED', 'CANCELLED') NOT NULL DEFAULT 'OPEN',
    opened_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    closed_at      DATETIME,
    note           VARCHAR(255),
    created_by     INT,                            -- user tạo đơn (phục vụ)
    CONSTRAINT fk_orders_table
        FOREIGN KEY (table_id) REFERENCES tables(id),
    CONSTRAINT fk_orders_user
        FOREIGN KEY (created_by) REFERENCES users(id)
);

-- Bảng chi tiết đơn hàng
CREATE TABLE order_details (
    id              INT AUTO_INCREMENT PRIMARY KEY,
    order_id        INT NOT NULL,
    menu_item_id    INT NOT NULL,
    quantity        INT NOT NULL,
    unit_price      DECIMAL(12,2) NOT NULL,
    total_price     DECIMAL(12,2) NOT NULL,
    item_status     ENUM('ORDERED', 'IN_KITCHEN', 'READY', 'SERVED', 'CANCELLED') NOT NULL DEFAULT 'ORDERED',
    note            VARCHAR(255),
    CONSTRAINT fk_order_details_order
        FOREIGN KEY (order_id) REFERENCES orders(id),
    CONSTRAINT fk_order_details_menu_item
        FOREIGN KEY (menu_item_id) REFERENCES menu_items(id)
);

-- Bảng hóa đơn (Bill) 1–1 với Order
CREATE TABLE bills (
    id             INT AUTO_INCREMENT PRIMARY KEY,
    order_id       INT NOT NULL UNIQUE,
    total_amount   DECIMAL(12,2) NOT NULL,     -- Tạm tính
    discount_pct   DECIMAL(5,2) DEFAULT 0,     -- Giảm giá %
    discount_value DECIMAL(12,2) DEFAULT 0,    -- Giảm giá trực tiếp
    final_amount   DECIMAL(12,2) NOT NULL,     -- Thành tiền cuối cùng
    payment_method ENUM('CASH', 'CARD', 'EWALLET', 'OTHER') NOT NULL,
    paid_at        DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    cashier_id     INT,                        -- Thu ngân
    CONSTRAINT fk_bills_order
        FOREIGN KEY (order_id) REFERENCES orders(id),
    CONSTRAINT fk_bills_cashier
        FOREIGN KEY (cashier_id) REFERENCES users(id)
);
```
